/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "placehold.co"],
    // remotePatterns: [
    //   {
    //     hostname: ["localhost", "placehold.co"],
    //   },
    // ],
  },
};

module.exports = nextConfig;
